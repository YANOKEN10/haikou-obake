# AGENTS.md — 廃校おばけ大作戦

このファイルは、AIエージェント（Codex / Claude Code など）がこのリポジトリで
作業するときの手引きです。人が読んでも分かるように書いてあります。

---

## このプロジェクトは何か

ブラウザだけで遊べる 3人称オープンワールド・サバイバルホラー（ちょっぴりコメディ）。
プレイヤーは廃校に住みつく **おばけ**。心霊スポット目当てに来る人間たちを
驚かせて、校門の外へ追い出す。

- 公開URL: https://haikou-obake-daisakusen.vercel.app/
- リポジトリ: https://github.com/YANOKEN10/haikou-obake （public）
- ホスティング: Vercel（`main` に push すると自動デプロイ）

### いちばん大事な前提

**遊ぶのは主に子ども**（メールアドレスを持っていない子でも遊べること、が出発点）。
URLを教え合って遊ぶ。この前提は、次の判断に直結する：

- アカウントは **なまえ＋あいことば** のみ。**メールアドレスは一切あつかわない**
- 個人情報（住所・電話番号・個人のメール）を画面に常設しない
- 性的な表現、こわがらせ以上の残酷な表現は入れない
- 課金は「任意の応援（寄付）」だけ。ゲーム内容に一切影響させない

---

## 技術のかたち

- **Three.js r0.169**（`lib/three.module.js` に同梱。CDNを使わない）
- **バニラ JS + ES modules + importmap**。**ビルド工程なし**
- **画像・音声ファイルを使わない**。形状はすべてコード生成、効果音はすべて WebAudio 合成
  （アイコンPNGだけ例外）
- サーバー側は **Vercel Serverless Functions**（`api/*.js`、CommonJS）＋ **Vercel Blob**

### 描画のしくみ（さわる前に知っておくこと）

廃校まるごとを **1つの静的メッシュにまとめて 1ドローコール**で描いている。
`src/meshbuild.js` の `MeshBuilder` が箱・板・草を1つのジオメトリに積む。

- 材質は `MeshPhongMaterial` + `vertexColors`
  （Lambert だと広い面の陰影がおかしくなったため）
- 光源は**プール方式**。近い人間・非常灯にだけ、決まった数のライトを割りあてる。
  ライトの数を動的に増減させると **シェーダが再コンパイルされてカクつく**ので、
  数は固定のまま位置だけ動かすこと
- 端末ごとの画質は `pickQuality()`（`src/main.js`）。草・林の量は `grass` 係数で調整

---

## ファイルの地図

| ファイル | 役割 |
|---|---|
| `src/main.js` | ゲーム全体の司令塔。ループ、入力、波の生成、交換所、通信 |
| `src/world.js` | 廃校の生成（校舎4階＋屋上／中庭／体育館／運動場／林／山／秘密の教室） |
| `src/player.js` | おばけ本体と3人称カメラ。7つのすがた（キャラ）の見た目 |
| `src/human.js` | 人間1人。移動・恐怖・ギャグ（転ぶ／上着が破ける）・服そう |
| `src/people.js` | 100グループ・84人の指名キャラ。性格17種と、その服そう |
| `src/entities.js` | 拾いもの・仕掛け・召喚おばけ・ネコ・告白・ともだちのおばけ |
| `src/data.js` | 材料9／仕掛け12／おばけ7／レア度7段／キャラ7／交換所の表 |
| `src/meshbuild.js` | 静的メッシュの組み立て（`box` `wall` `slab` `blade` `tuft`） |
| `src/util.js` | 当たり判定（空間ハッシュ）、経路探索、**種つき乱数** |
| `src/ui.js` | HUD・おばけ工房・交換所・部屋の画面 |
| `src/home.js` | ホーム画面（あそぶ／ログイン／プロフィール／ともだち） |
| `src/net.js` | ともだちと遊ぶ（1秒に1回のやりとり＋あいだを補間） |
| `src/support.js` | 応援（寄付）ボタン。**ここのURLが空ならボタンごと出ない** |
| `api/auth.js` | なまえ＋あいことばの登録・ログイン |
| `api/save.js` | 記録の読み書き |
| `api/room.js` | ともだちと遊ぶ「部屋」 |
| `api/friends.js` | ともだち申請・プロフィール・さそい |
| `api/_lib.js` | サーバー共通（scryptハッシュ、HMAC券、CORS、Blob読み書き） |

---

## 動かしかた

```bash
node serve.js          # http://localhost:5178
```

ビルドは不要。`serve.js` は静的配信 + 開発用のスクショ受け口（`/__shot`）だけ。

**サーバー機能（ログイン・ともだち・部屋）はローカルでは動かない。**
Vercel Blob が必要なため。公開URLで確認すること。

### 環境変数（Vercel側）

| 名前 | 用途 |
|---|---|
| `AUTH_SECRET` | ログイン券の署名鍵。**16文字以上。代用値は用意していない** |
| `BLOB_READ_WRITE_TOKEN` | Vercel Blob（Vercelが自動で入れる） |

`_lib.js` の `configured()` が両方そろっているか確認し、なければ 503 を返して
「この端末だけ」モードに誘導する。**鍵をコードに書かないこと。**

---

## 確認のしかた（大事）

このプロジェクトは**目で見て確かめられる**ことを重視している。

1. `serve.js` を起動
2. ブラウザで `window.game` を触ってゲームを進める
   （画面が隠れていると `requestAnimationFrame` が止まるので、
   `game.update(dt, t)` を手で回すこと。**`t` を渡し忘れると NaN になる**）
3. 見た目は `/__shot?n=なまえ` に PNG を POST して `shots/` に保存し、読み返す

```js
g.renderer.render(g.scene, g.camera);
await fetch("/__shot?n=test", { method: "POST",
  body: g.renderer.domElement.toDataURL("image/png") });
```

サーバー側は本番に対してテストを流せる：

```bash
node tools/nettest.mjs    # 部屋のやりとり
node tools/frtest.mjs     # ともだち機能（テスト用アカウントは自動で消す）
```

---

## 書きかたの約束

- **コメントと画面の文字は日本語**。子どもが読む画面は、ふりがな不要でも
  やさしい言い回しにする（「回収」より「もどす」など）
- 変数名は英語、コメントは日本語
- **「なぜそうしたか」をコメントに残す**。とくに、一度ハマって直した箇所
- 1ファイルを大きくしすぎない。`world.js` は既に大きいので、
  新しい建物は関数に切って足す

### ハマりやすいところ

- **開口部（ドア・窓）の上に板を描かない**。`siding` / `posts` / `wainscot` /
  `ivy` は必ず `holes` を渡す。渡し忘れると「通れるのに壁に見える」不具合になる
- **窓の穴を「床まで」広げるときは、もとの窓を先に消す**。
  `wallWithHoles` は開口の下に**腰壁の当たり判定**（高さ0〜0.9）を残すので、
  ふつうの窓に重ねて床までの穴を足すと「見た目は通れるのに歩けない」不具合になる
  （割れた窓が実際にこれで通れなかった）
- **逃走中の人間を「門へまっすぐ引っぱる」処理を書かない**。壁を突きぬけて
  部室棟の中に入り、かえって出られなくなる。引っぱるなら**経路の次の地点へ**、
  かつ必ず `colliders.resolve` を通すこと
- **人間の体は `["stair","furn","soft"]` をすりぬける**。経路探索（`navSight`）が
  家具を無視して線を引くので、体だけ家具に当たると花壇の前で足ぶみになる。
  この2つは必ず同じ扱いにすること
- **`THREE.Color.lerp` は 0〜1 の外に出ると色が飛ぶ**。必ず clamp する
- **階段**は U字（西の段→中おどり場→東の段）。`stairSurface()` が段の高さを返し、
  歩くだけで自然に登る。x を切りかえないと中おどり場で止まる
- **ともだちと遊ぶとき**、人間の番号（`hid`）は おやとお客さんで一致させる。
  名まえが重なっても **番号はずらさない**。人数をしぼるときも `hidNext` は
  必ず進める（おやが送る `rooms` でお客さんが同じ人数を再現する）
- **ネットの動きは「予測」で滑らかにする**。届いた位置へ寄せるだけだと、
  1回届くたびにワープして止まって見える。届いた位置＋速度×経過秒 を
  予測して、そこへ寄せること（`net.js` の `PREDICT_MAX`、
  `human.js` の `NET_PREDICT_MAX`）
- **ホストの人間スナップショットは「新しく届いたときだけ」当てる**。
  毎フレーム当てると `netAge` が 0 に戻って予測が消え、カクカクになる
  （`net.worldSeq` と `game._worldSeen` で見分ける）
- **落としものは `clampPlay()` を通す**。人間は門の外まで歩いて帰るので、
  そのまま落とすとフェンスの外に材料が落ちて取れなくなる
- **`ui.js` に呼ばれているメソッドが実在するか確かめる**。
  `openRoom`/`closeRoom`/`setRoom`/`roomMsg` が無いまま呼ばれていて、
  「ともだちと あそぶ」を押すと画面が固まっていた
- **種つき乱数**（`seedRandom`）は、部屋の全員に同じ人が同じ順で来るようにするため。
  `Roster` は自分専用の乱数を持つ（ゲーム中の乱数消費でズレないように）

---

## いま入っている遊び

- 校舎4階＋屋上、中庭、体育館（切妻屋根・ステージ・ピアノ）、運動場、
  **すりぬけでしか行けない秘密の教室**
- 出入口4か所（正門・東の通用門・西の裏門・北の勝手口）からランダムに人間が来る
- **人間はマップに最大15人**（`MAX_ALIVE`）。時間がたつほど次の組が早く来る
  （`nextWaveGap()`）。`spawnWave(room)` の `room` が「あと何人入れるか」
- **人間の名まえを押すと、その人を さがせる**（`trackHid`／`updateFind()`）。
  光の柱は `depthTest:false` でかべごしに見える
- 校舎への入りくちも4か所（昇降口・裏口・割れた窓 東西）。`WAYS`（`world.js`）
- グループごとに歩きかたが変わる（`planGroup` in `main.js`）。
  1列でゆっくり／はぐれる子／トイレに寄る子／体育館に寄り道／速さのばらつき
- 指名キャラ84人＋一般。100グループが順番に来て、一周するまで重複しない
- 仕掛け12種・召喚おばけ7種・材料9種
- **レア度7段（白・水色・青・赤・紫・銀・金）**。へんぴな場所ほど良い色が出る
- **かけら**を交換所で、仕掛け・おばけ・**すがた（キャラ7体）**と交換
- ともだち機能（申請・プロフィール・さそい）とマルチプレイ（4人まで）。
  ともだちのおばけは、その人が選んだ**すがた**で見える（`buildGhostLook`）
- ともだちと**はさみうち**（人間をはさんで反対がわ）でこわさ2.1倍＋材料どっさり
- だれかがおどかすと**全員の画面に材料が1つ落ちる**（`giveShare()`）。
  拾いものは画面ごとに別なので、取りあいにはならない
- 隠し要素：赤いワンピースの女の子、トイレのうんこ、運動場のネコ、体育館裏の告白

---

## お金まわり（さわるときは慎重に）

- いまあるのは **応援（寄付）ボタンだけ**。売る商品はない
- `src/support.js` の `SUPPORT_URL` が空のあいだは **ボタンごと出ない**
- 応援しても **ゲームの中身は一切変わらない**（お礼の表示が変わるだけ、端末内のみ）
- カード情報はStripeの画面だけであつかう。このゲームは受けとらない
- **ガチャ・かけらの直接販売・サブスクは入れない**。子どもが主な遊び手のため

---

## やらないこと

- CDN・外部APIへの依存を増やす（オフラインでも遊べる状態を壊さない）
- ビルド工程の導入
- 画像・音声ファイルの追加（アイコンを除く）
- メールアドレスの収集
- 個人情報を画面に常設する
