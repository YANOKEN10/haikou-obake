# 「☁ どの端末でも」を動かすための準備（Vercel 側の設定）

なまえ＋あいことばでログインし、記録をサーバーにあずける機能です。
**メールアドレスは一切使いません。**（ボンバーマンのゲームと同じ作りです）

コード（`api/` と `src/cloud.js`）はすでに書き終わっています。
あとは Vercel 側で保管庫を1つつなぐだけで動きます。

---

## 1. 記録の保管庫（Blob ストア）をつなぐ

1. <https://vercel.com/dashboard> → プロジェクト **haikou-obake-daisakusen** を開く
2. 上のタブ **Storage** → **Create Database**（または既存のストアの **Connect**）
3. **Blob** を選ぶ
   - 名前は `haikou-obake-blob` など、分かるもので
   - Region は Tokyo などお近くに
4. 作成したら **Connect to Project** で `haikou-obake-daisakusen` を選び、
   環境（Production / Preview / Development）は**すべて**にチェック

> これで `BLOB_READ_WRITE_TOKEN` という環境変数が自動で設定されます。
> ボンバーマンで作ったストアを使い回すこともできます（その場合も Connect が必要です）。

## 2. 署名用の合言葉を1つ足す

1. プロジェクトの **Settings** → **Environment Variables**
2. **Add New**
   - Key: `AUTH_SECRET`
   - Value: 長いランダムな文字列（40文字以上を推奨。人に見せない値です）
   - Environments: **すべて**にチェック
3. **Save**

> この値でログインの券に署名します。あとから変えると、
> みんなが一度ログインし直すことになるので、決めたら変えないでください。

## 3. 再デプロイ

**Deployments** → 最新のものの「…」→ **Redeploy**
（または私が `git push` すれば自動で反映されます）

## 4. 動作確認

私が公開URLで次を確認します。

- とうろく → ログアウト → ログイン
- 記録をあずける → 別の端末でとりだす
- まちがったあいことばで弾かれること
- 他人の記録が見えないこと

---

## しくみ（安全面）

| | |
|---|---|
| あいことば | そのまま保存しません。利用者ごとの乱数を混ぜて `scrypt` でハッシュ化 |
| 照合 | 時間差から中身が漏れない比較方法（timingSafeEqual） |
| まちがえたとき | わざと少し待たせて、総当たりを遅くする |
| ログイン状態 | HMAC-SHA256 で署名した券。1年で切れる |
| 記録 | 非公開の Blob に、利用者1人＝1ファイル。ファイル名も推測できないハッシュ |
| 呼び出せる場所 | 公開URLと localhost だけに限定（CORS） |
| 記録の大きさ | 1人200KBまで |

## 気をつけること

- **あいことばを忘れると、あずけた記録は取り出せません。**
  メールを使わない代わりに、再発行の手段がありません。
- 子どもには「📱 この端末だけ」を案内してください。あいことばの管理が要りません。
