# Codex への 引きつぎ — 廃校おばけ大作戦

このファイルを そのまま Codex に 見せれば、続きから 作業できます。
**まず [AGENTS.md](AGENTS.md) を 読んでください。** 設計の理由と、
ハマりやすい所が 書いてあります。

---

## 1. これは 何か

ブラウザだけで あそべる 3人称の オープンワールド・ホラー（すこしコメディ）。
プレイヤーは 廃校に すみつく **おばけ**。心霊スポット目当てに 来る人間を
おどかして 校門の外へ 追いだす。

| | |
|---|---|
| 公開URL | https://haikou-obake-daisakusen.vercel.app/ |
| リポジトリ | https://github.com/YANOKEN10/haikou-obake （public） |
| ホスティング | Vercel（`main` に push すると 自動デプロイ） |
| サーバー地域 | 東京 `hnd1`（`vercel.json`） |

### いちばん大事な前提

**あそぶのは 主に 子ども**（メールアドレスを 持っていない子でも あそべること）。

- アカウントは **なまえ＋あいことば** のみ。**メールアドレスは 一切あつかわない**
- 個人情報を 画面に 常設しない
- 性的な表現、こわがらせ以上の 残酷な表現は 入れない
- 課金は「任意の応援（寄付）」だけ。ゲーム内容に 一切影響させない

---

## 2. データは どこにあるか

### 2-1. ソースコード（このリポジトリ）

```
haikou-obake/
├ index.html          画面の HTML と CSS が ぜんぶ ここ（1ファイル）
├ serve.js            開発用サーバー（node serve.js → localhost:5178）
├ vercel.json         サーバーの 地域（hnd1＝東京）
├ lib/three.module.js Three.js r0.169（CDNを 使わず 同梱）
│
├ src/                ゲーム本体（ES modules、ビルド不要）
│  ├ main.js          司令塔。ループ・入力・波の生成・交換所・通信のまとめ
│  ├ world.js         廃校の生成（4階＋屋上／中庭／体育館／運動場／林／山／秘密の教室）
│  ├ player.js        おばけ本体と3人称カメラ。7つの すがたの 見た目・色がえ
│  ├ human.js         人間1人。移動・恐怖・ギャグ・服そう・通信での なめらか化
│  ├ people.js        100グループ／指名キャラ84人／名まえ184種／性格18種
│  ├ entities.js      拾いもの・仕掛け・召喚おばけ・ネコ・告白・ともだちのおばけ
│  ├ data.js          材料9／仕掛け12／おばけ7／レア度7段／キャラ7／きょうか／色53
│  ├ battle.js        おどかし勝負（ともだちとのバトル）
│  ├ net.js           ともだちと あそぶ（通信のまとめ）
│  ├ rtc.js           ブラウザ同士を 直接つなぐ（WebRTC）★新しい
│  ├ meshbuild.js     静的メッシュの 組み立て
│  ├ util.js          あたり判定・道さがし・種つき乱数
│  ├ ui.js            HUD・おばけ工房・交換所・きょうか・色がえ・部屋・勝負
│  ├ home.js          ホーム画面（あそぶ／ログイン／プロフィール／ともだち）
│  ├ cloud.js         なまえ＋あいことばの ログイン、記録の クラウド保存
│  ├ save.js          この端末への 保存
│  ├ sky.js           空と 星
│  ├ audio.js         効果音（ぜんぶ WebAudio 合成。音声ファイルなし）
│  ├ touch.js         スマホ・タブレットの 操作
│  └ support.js       応援（寄付）ボタン ★URLが空なら ボタンごと 出ない
│
├ api/                Vercel Serverless Functions（CommonJS）
│  ├ _lib.js          共通（scryptハッシュ・HMAC券・CORS・Blob読み書き）
│  ├ auth.js          なまえ＋あいことばの 登録・ログイン
│  ├ save.js          記録の 読み書き
│  ├ room.js          ともだちと あそぶ「部屋」＋WebRTCの あいさつ中つぎ
│  └ friends.js       ともだち申請・プロフィール・さそい
│
├ tools/
│  ├ nettest.mjs      部屋のやりとりを 本番に対してテスト
│  └ frtest.mjs       ともだち機能を テスト
│
├ AGENTS.md           ★開発の 手引き。ハマりやすい所の 一覧
├ README.md           あそびかた
└ HANDOFF.md          このファイル
```

### 2-2. 保存されている データ

| どこ | なに | かたち |
|---|---|---|
| ブラウザの localStorage | この端末の 記録（材料・すがた・きょうか・色） | `src/save.js` の `blank()` が かたち |
| Vercel Blob `hobake/user/<なまえ>.json` | アカウントと あずけた記録 | `api/auth.js` `api/save.js` |
| Vercel Blob `hobake/room/<あいことば>.json` | 部屋（だれがいる・位置・あいさつ） | `api/room.js` |
| Vercel Blob `hobake/room/<あいことば>.w.json` | 人間たちの ようす（おやだけが書く） | 同上 |
| Vercel Blob `hobake/fr/<なまえ>.json` | ともだち関係 | `api/friends.js` |

**セーブデータの かたち**は `src/save.js` の `blank()` を 見てください。
`upg`（すがたごとの きょうか）、`paints`（手に入れた色）、
`paint`（すがたごとに ぬった色）が 新しく 入っています。

### 2-3. 環境変数（Vercel側。**コードに 書かないこと**）

| 名前 | 用途 |
|---|---|
| `AUTH_SECRET` | ログイン券の 署名鍵。16文字以上。代用値なし |
| `BLOB_READ_WRITE_TOKEN` | Vercel Blob（Vercelが 自動で入れる） |

`api/_lib.js` の `configured()` が 両方そろっているか 確かめ、
なければ 503 を返して「この端末だけ」モードに 誘導します。

---

## 3. 動かしかた・確かめかた

```bash
node serve.js          # http://localhost:5178
```

ビルド不要。**サーバー機能（ログイン・ともだち・部屋）は ローカルでは 動きません**
（Vercel Blob が 要るため）。公開URLで 確かめてください。

### 目で見て 確かめる（このプロジェクトの やりかた）

画面が 隠れていると `requestAnimationFrame` が 止まるので、
`game.update(dt, t)` を 手で 回します。**`t` を 渡し忘れると NaN に なります。**

```js
// ブラウザの コンソールで
const g = window.game;
g.startGame(false);
let T = 0;
const real = g.renderer.render; g.renderer.render = () => {};   // 絵を止めて 速く回す
for (let i = 0; i < 60 * 120; i++) { T += 1/60; g.update(1/60, T); }
g.renderer.render = real;
```

見た目は `/__shot?n=なまえ` に PNG を POST して `shots/` に 保存し、読み返します。

```js
g.renderer.render(g.scene, g.camera);
await fetch("/__shot?n=test", { method: "POST",
  body: g.renderer.domElement.toDataURL("image/png") });
```

サーバー側は 本番に対して テストを 流せます。

```bash
node tools/nettest.mjs    # 部屋のやりとり
node tools/frtest.mjs     # ともだち機能（テスト用アカウントは 自動で消す）
```

---

## 4. いま どこまで できているか

### 入っている あそび

- **ステージ選択**：夕暮れ木造校舎／1000人超で霧ヶ丘分校／2000人超で月影廃遊園地。
  選んだマップだけを生成し、新2マップはそれぞれ静的1メッシュにまとめている。
- 4階建ての 木造校舎＋屋上、中庭、体育館、運動場、**すりぬけでしか行けない秘密の教室**
- 出入口4か所（正門・東の通用門・西の裏門・北の勝手口）＋
  校舎への 入りくち4か所（昇降口・裏口・割れた窓 東西）から ランダムに 来る
- 人間は **マップに最大15人**。時間がたつほど 次の組が 早く来る
- 指名キャラ84人＋一般。名まえ184種。100グループが 順ばんに 来る
- 仕掛け12種・召喚おばけ7種・材料9種
- レア度7段（白・水色・青・赤・紫・銀・金）。へんぴな場所ほど よい色
- 交換所で 仕掛け・おばけ・**すがた（7体）**と 交換
- **すがたを きたえる**：すがたごとに はやさ／ダッシュ／こわさ／とどく／すりぬけ を Lv30まで
- **色をかえる**：パーツ（からだ／かざり／ひかり／め）ごとに 53色から
- **そとでは 32m まで 浮ける**（屋根は 14.4m）
- 人間の名まえを 押すと **その人を さがせる**（光の柱＋やじるし）
- ともだち機能（申請・プロフィール・さそい）とマルチプレイ（4人まで）
- **おどかし勝負**（3・2・1 → 制限時間 → けっか発表 → ごほうび）
- 隠し要素：赤いワンピースの女の子、トイレのうんこ、運動場のネコ、体育館裏の告白

### お金まわり（さわるときは 慎重に）

- いま あるのは **応援（寄付）ボタンだけ**。売る商品は ない
- `src/support.js` の `SUPPORT_URL` が **空のあいだは ボタンごと 出ない**
- **この3つの値は、持ち主が 自分で 決めます。勝手に 入れないでください。**
  - `SUPPORT_URL`（Stripeの 支払いリンク）
  - `CONTACT_MAIL`
  - `TOKUSHOHO_URL`
- ガチャ・かけらの直接販売・サブスクは 入れない（子どもが 主な あそび手のため）

---

## 5. いま やりかけの こと（★ここから 続けてください）

### 5-1. WebRTC（ブラウザ同士の 直接つなぎ）— **動作確認 とちゅう**

**なぜ 入れたか。** 本番で 通信の 往復を はかったところ、

| 内わけ | 時間 |
|---|---|
| ブラウザ → 関数 → もどる（US配置のとき） | 622ms |
| 同（東京 `hnd1` に 変えたあと） | **256ms** |
| Vercel Blob の 読み書き | **+750〜1150ms** |
| **合計（sync 1往復）** | **約1.1〜1.4秒** |

つまり ともだちの位置は **1.4秒に1回**しか とどいていませんでした。
どんなに よそうを うまくしても、これでは なめらかに なりません。
**Vercel Blob は ファイル置き場で、リアルタイム通信用では ない**のが 根本原因です。

**やったこと。**

1. `vercel.json` で サーバーを 東京（`hnd1`）に した → 関数までの 往復 622→256ms
2. `api/room.js` の Blob 読み書きを `Promise.all` で 同時にした → sync 1373→1117ms
3. **`src/rtc.js` を 新しく作り、ブラウザ同士を 直接つなぐようにした**
   - あいさつ（SDP・ICE）は `api/room.js` の ポスト（`sigIn`）ごしに やりとり
   - つながれば 30〜80ms、1秒に20回。`ordered:false, maxRetransmits:0`
   - つながったら サーバーごしは 2.5秒に1回の なかま確認だけに 落とす
   - 12秒 まっても つながらなければ あきらめて、これまでの やりかたに もどる
   - STUN は Google の 無料サーバー（`stun:stun.l.google.com:19302`）
     ※ AGENTS.md の「外部依存を 増やさない」から 外れる ゆいいつの点。
       持ち主の 了解ずみ。使えなくても ゲームは これまでどおり あそべる。

**★のこっている 作業（ここから お願いします）**

- [ ] **2つの端末（またはタブ）で ほんとうに つながるか の 確認**
      片方で 部屋を作り、もう片方が あいことばで 入る。
      `game.net.rtc.links` に 相手が 入り、`.ready === true` に なるか。
      つながると 画面に「⚡ ◯◯ と 直接つながりました」と 出ます。
- [ ] つながったあと、**ともだちの動きが なめらかか** 目で見て 確かめる
- [ ] つながらない ネットでの **もどり（フォールバック）**の 確認
      → `src/rtc.js` の `ICE` を 空配列 `[]` に して ためすと 再現しやすい
- [ ] TURN が ないので、きびしい NAT では つながりません。
      体感 1割ほど。必要なら TURN の 導入を 検討（有料サービスが 要ります）

**確かめかたの ヒント**

```js
// 両方の タブで
const g = window.game;
g.net.rtc.supported          // WebRTC が 使えるか
g.net.rtc.links              // Map。相手の pid → {ready, ch, pc}
[...g.net.rtc.links.values()].map(l => l.ready)   // [true] なら つながっている
[...g.net.peers.values()].map(p => ({name:p.name, fast:p.fast}))  // fast:true なら 直接
```

### 5-2. Stripe の 支払いリンク — **持ち主の 作業まち**

`src/support.js` の `SUPPORT_URL` に 支払いリンクを 1行 入れるだけで
応援ボタンが 出ます。**Codex が 勝手に 入れないでください。**

---

## 6. ハマりやすい所（AGENTS.md の 抜き書き）

ここは 実際に 踏んで 直した所です。同じ所で つまずかないように。

### 描画

- 廃校まるごとを **1つの静的メッシュに まとめて 1ドローコール**で 描いている
- 光源は **プール方式**。数を 動的に 増減させると **シェーダが再コンパイルされて カクつく**
- **開口部（ドア・窓）の 上に 板を 描かない**。`siding`/`posts`/`wainscot`/`ivy` は
  必ず `holes` を 渡す
- **窓の穴を「床まで」広げるときは、もとの窓を 先に 消す**。
  `wallWithHoles` は 開口の下に **腰壁の当たり判定**（高さ0〜0.9）を 残す
- **`THREE.Color.lerp` は 0〜1 の外に出ると 色が飛ぶ**。必ず clamp
- **`CHARS` を 書きかえない**。みんなで使う表。きょうかは `player.stat(key)` で足す

### 道さがし・移動

- **`navSight` は 人の「はば」も 見る**。まんなか1本だけだと、窓の開口の はしを
  かすめる 斜めの近道まで「通れる」と 判定し、体に幅がある人間は 壁の角に当たって
  外壁ぞいへ 押し出され、そのまま 動けなくなる。
  ただし 細い柱は よけられるので、「よこの線が `SIDE_RUN`(0.7m) 以上 つづけて
  ふさがれている」ときだけ 落とす。**階段の段は 除外**（入れないと 上の階が全滅）
- **「動けているか」ではなく「近づけているか」で 詰まりを見る**（`slipT`）。
  壁に 押されて 横すべりしている間は 移動量が あるので 見のがす
- **経路が 見つからなかったら 長く待たない**。`goTo` は 成否を返す。
  false のときは `stateT` を 0.6〜1.6秒に して すぐ 考えなおす
- **屋上では `minTop` を 渡す**。渡さないと 4階の壁（上端が 屋根と同じ 14.4m）に
  ぶつかって 見えない壁に なる
- **落としものは `clampPlay()` を 通す**。人間は 門の外まで 歩いて 帰るので、
  そのまま 落とすと フェンスの外に 材料が 落ちて 取れなくなる
- **召喚おばけは 壁ぞいに すべって よける**。まっすぐ 向かうだけだと 壁で 止まる

### 通信

- **速度は 本人が 測って 送る**。到着間隔から 逆算すると 通信のゆらぎが
  そのまま 速度の誤差に なる
- **「速度で動かす＋ズレ直しに 速度制限」**。大事なのは 2つ。
  ① 画面上の相手は **常に本人の速度で 進み続ける**（止まらない）
  ② 届いた位置とのズレは 直すが、**直す速さに 上限**（`FIX_MAX`=4.0）
  「届いた位置へ 寄せるだけ」は ワープ→静止に、
  「上限なしで 寄せる」は 加速→減速の うねりに なる。どちらも 試して 駄目だった
- **`WARP`(20) を 小さくしない**。走って旋回する相手で 予測がずれた分が
  閾値を超え、カクッと飛ぶ
- **ホストの人間スナップショットは「新しく届いたときだけ」当てる**。
  毎フレーム当てると 予測が 消えて カクカクになる（`net.worldSeq` と `game._worldSeen`）
- **人間の番号（`hid`）は おやとお客さんで 一致させる**。名まえが重なっても
  **番号は ずらさない**。人数をしぼるときも `hidNext` は 必ず 進める
- **`ui.js` に 呼ばれているメソッドが 実在するか 確かめる**。
  `openRoom`/`closeRoom`/`setRoom`/`roomMsg` が 無いまま 呼ばれていて、
  「ともだちと あそぶ」を 押すと 画面が 固まっていた
- **勝負のおわりは、状態を書きかえる前に 判定する**。先に `phase="over"` にすると
  `finish()` が「勝負中でなかった」と見て けっかを出さない

### そのほか

- **`pick(配列, たね)` は 必ず `mix()` を 通す**。たねを そのまま `% 長さ` すると、
  たねの作りかた（`g*13 + i*5 + 1`）と 配列長の 相性で 数とおりしか 出ない。
  実際、名まえが 52個あるのに 13個しか出ず、カエデが 100グループ中 50回 出ていた
- **種つき乱数**（`seedRandom`）は 部屋の全員に 同じ人が 同じ順で 来るようにするため
- **階段**は U字。`stairSurface()` が 段の高さを返す。x を切りかえないと 中おどり場で止まる

---

## 7. 書きかたの 約束

- **コメントと 画面の文字は 日本語**。子どもが読む画面は やさしい言い回しに
  （「回収」より「もどす」など）
- 変数名は 英語、コメントは 日本語
- **「なぜ そうしたか」を コメントに 残す**。とくに 一度ハマって 直した所
- 1ファイルを 大きくしすぎない。`world.js` は 既に 大きいので、
  新しい建物は 関数に 切って 足す
- **やらないこと**：ビルド工程の導入／画像・音声ファイルの追加（アイコン除く）／
  メールアドレスの収集／個人情報を 画面に 常設

---

## 8. 直近の コミット（新しい順）

```
ブラウザ同士を 直接つなぐ（WebRTC）              ← ★動作確認 とちゅう
sync の 通信を 速くする（置き場の 読み書きを 同時に）
サーバーの ばしょを 東京（hnd1）に する
ともだちの動きの「ワープ→静止」を なおす
色がえ：えらんだ色に ぴったり なるようにし、色を ふやす
ともだちと 同じ画面で 遊べるようにする
名まえが かたよる 不具合を なおし、名まえを ふやす
外壁に はりついて 動けなくなる 不具合を なおす
すがたの 色を かえられるようにする
すがたを きたえられるようにする（レベルアップ）
おどかし勝負（ともだちとの バトル）を 足す
```

`git log --oneline -30` で もっと 見られます。
コミットメッセージに「なぜ そうしたか」を 日本語で 書いてあるので、
そこを 読むのが いちばん 早いです。
